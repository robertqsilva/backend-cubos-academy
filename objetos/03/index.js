const usuario = {
    nome: "Robert",
    idade: 18,
    profissao: "programador",
    cor_preferida: "preto",
};

const { nome, idade, profissao, cor_preferida } = usuario;

console.log(`Olá! Eu sou ${nome} e tenho ${idade} anos. Sou ${profissao} e minha cor preferida é ${cor_preferida}`);

