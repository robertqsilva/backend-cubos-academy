const raio = 1;

const circuferencia = 2 * Math.PI * raio;
const areaDoCirculo = Math.PI * raio ** 2;

console.log(`O circulo tem ${circuferencia} de circuferencia`);
console.log(`O circulo tem ${areaDoCirculo} de aréa`);