const raioDaBase = 3;
const alturaDoCilidro = 1;

const formula = 2 * raioDaBase * (raioDaBase + alturaDoCilidro) + "pi";
const resultado = formula;

console.log(`Àrea total do cilinfro é aproximadamente ${resultado}`);
