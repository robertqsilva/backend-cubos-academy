function solucao(numero, limiteInferior, limiteSuperior) {

    const intervalo = (numero >= limiteInferior && numero <= limiteSuperior)
    const validacaoIntevalo = (intervalo) ? "PERTENCE" : "NAO PERTENCE"
    console.log(validacaoIntevalo)

}